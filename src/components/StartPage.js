import React from 'react'

export default function StartPage() {
    return (
        <div className="startpage--container">
            <p className="startpage--title">Quizzical</p>
            <button className="startpage--button">Start quiz</button>
        </div>
    )
}